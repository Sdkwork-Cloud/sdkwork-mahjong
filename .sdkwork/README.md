# SDKWork Workspace

Source-controlled workspace metadata for `sdkwork-mahjong`, governed by `../sdkwork-specs/SDKWORK_WORKSPACE_SPEC.md`.

- `skills/`: repository-local agent skills
- `plugins/`: repository-local plugins

This directory is not runtime state.
