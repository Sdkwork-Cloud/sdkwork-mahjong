# Packages

Shared TypeScript contracts consumed across the mahjong workspace.

| Package | Purpose |
| --- | --- |
| `packages/common/mahjong/sdkwork-mahjong-contracts` | Cross-surface DTOs and helpers using `@sdkwork/utils` |

Architecture-local packages for PC UI live under `apps/sdkwork-mahjong-pc/packages/`.
